<h1>Courses</h1>
<p><%= link_to 'Create course', new_course_path %></p>
<% @courses.each do |course| %>
<p><%= link_to course.title, course_path(course) %> <%= course.start_date.to_formatted_s(:short) %></p>
<% end %>
